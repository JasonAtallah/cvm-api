{
  "method": "POST",
  "header": [
    {
      "key": "Content-Type",
      "value": "application/json"
    },
    {
      "key": "Authorization",
      "value": "Bearer {{BUYER_TOKEN}}"
    }
  ],
  "body": {
    "company": {
      "name": "{{vendor.company.name}}",
      "city": "{{vendor.company.city}}"
    },
    "contact": {
      "firstName": "{{vendor.contact.firstName}}",
      "email": "{{vendor.contact.email}}"
    }
  },
  "url": {
    "raw": "{{API_HOST}}/buyer/v1/vendors",
    "host": [
      "{{API_HOST}}"
    ],
    "path": [
      "buyer",
      "v1",
      "vendors"
    ]
  }
}