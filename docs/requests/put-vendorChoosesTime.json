{
  "method": "PUT",
  "header": [
    {
      "key": "Content-Type",
      "value": "application/json"
    },
    {
      "key": "Authorization",
      "value": "{{BUYER_TOKEN}}"
    }
  ],
  "body": {
    "selectedTime": {
      "location": {
        "name": "{{selectedTime.location.name}}",
        "address": "{{selectedTime.location.address}}",
        "zip": "{{selectedTime.location.zip}}",
        "city": "{{selectedTime.location.city}}",
        "state": "{{selectedTime.location.state}}"
      },
      "duration": "{{selectedTime.duration}}",
      "startDate": "{{selectedTime.startDate}}"
    }
  },
  "url": {
    "raw": "{{API_HOST}}/vendor/v1/vendors/{{VENDOR_ID}}/actions/VendorChoosesTime",
    "host": [
      "{{API_HOST}}"
    ],
    "path": [
      "vendor",
      "v1",
      "vendors",
      "{{VENDOR_ID}}",
      "actions",
      "VendorChoosesTime"
    ]
  }
}